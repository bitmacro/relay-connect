/**
 * Public marketing name vs npm scope — keep version in sync with package.json.
 */
export const RELAY_CONNECT_PRODUCT_NAME = "BitMacro Connect" as const;
export const RELAY_CONNECT_PACKAGE_NAME = "@bitmacro/relay-connect" as const;
export const RELAY_CONNECT_VERSION = "0.1.1" as const;
